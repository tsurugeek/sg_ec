.row
  - @products.each do |product|
    .col-sm-6.col-md-4
      .thumbnail
        = image_tag product.product_image_url(:thumb)
        .caption
          %h3
            = link_to product.name, new_product_cart_products_path(product)
